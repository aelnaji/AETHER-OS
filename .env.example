# AETHER-OS Environment Variables
#
# This file documents ALL supported environment variables.
# Create one of the following from these examples:
#   - Local dev (recommended): .env.local
#   - Docker staging:          .env.staging
#   - Docker production:       .env.prod
#
# Notes:
# - Variables prefixed with NEXT_PUBLIC_ are embedded into the frontend bundle at build time.
#   When building Docker images, set these as build args (see docker-compose.*.yml).
# - Never commit real secrets (.env.local, .env.staging, .env.prod).

# -----------------------------------------------------------------------------
# App / Runtime
# -----------------------------------------------------------------------------
# Controls behavior for env validation and health checks.
# Allowed: development | staging | production
AETHER_ENV=development

# Optional: host port mapping used by docker compose files.
AETHER_OS_PORT=3000
AETHER_OS_STAGING_PORT=3002

# Optional: displayed in health endpoints.
NEXT_PUBLIC_APP_VERSION=0.1.0

# -----------------------------------------------------------------------------
# Bytebot Backend (Socket.IO)
# -----------------------------------------------------------------------------
# Required in staging/production unless AETHER_ALLOW_NO_BYTEBOT=true.
# Examples:
#   - Local:  http://localhost:3001
#   - Docker: http://bytebot-agent:3001
NEXT_PUBLIC_BYTEBOT_ENDPOINT=http://localhost:3001

# Allow running without Bytebot in production (tools will fall back to simulated behavior).
AETHER_ALLOW_NO_BYTEBOT=true

# -----------------------------------------------------------------------------
# NVIDIA API (LLM)
# -----------------------------------------------------------------------------
# Optional server-side default API key. Users can also provide an API key via Settings.
NVIDIA_API_KEY=

# Public NVIDIA API base URL. Prefer NEXT_PUBLIC_NVIDIA_API_ENDPOINT.
NEXT_PUBLIC_NVIDIA_API_ENDPOINT=https://integrate.api.nvidia.com/v1

# Legacy alias (supported for backwards compatibility).
NEXT_PUBLIC_LLM_ENDPOINT=https://integrate.api.nvidia.com/v1

# Default model used in UI/settings.
NEXT_PUBLIC_DEFAULT_MODEL=meta/llama-3.1-405b-instruct

# -----------------------------------------------------------------------------
# Chat / UX
# -----------------------------------------------------------------------------
# Enable Server-Sent Events streaming from /api/chat.
NEXT_PUBLIC_CHAT_STREAMING=true

# -----------------------------------------------------------------------------
# Database (optional)
# -----------------------------------------------------------------------------
# Only used for health/readiness checks in this repo (frontend has no Prisma).
DATABASE_URL=postgresql://aether:aether_local@localhost:5432/bytebot

# -----------------------------------------------------------------------------
# Health Checks
# -----------------------------------------------------------------------------
# Timeout used for dependency checks (Bytebot/DB).
HEALTHCHECK_TIMEOUT_MS=5000

# Minimum free memory required to report readiness.
MIN_FREE_MEMORY_MB=128
